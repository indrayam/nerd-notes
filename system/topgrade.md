# topgrade

## Useful commands

```bash
# On MacOS
# Disable specific steps - same options as the command line flag
disable = ["system", "helm", "mas", "pnpm"]

# On Ubuntu
# Disable specific steps - same options as the command line flag
disable = ["emacs", "containers", "git_repos", "snap", "firmware", "restarts"]

```